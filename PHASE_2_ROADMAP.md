# PetPro Connect — Phase 2 Roadmap

*Created 2026-07-15 at founder direction. This is a **living document** — we add to it as development continues, using the same status legend and update habits as `ROADMAP.md`. The Phase 1 tracker (`ROADMAP.md`) remains the source of truth until the Week 8 demo ships; **nothing in this file starts before then** unless the founder explicitly pulls an item forward.*

---

## How This Works

Phase 2 is organized into **workstreams** instead of fixed weeks, because scope will keep growing. Each workstream ends with a **QA checkpoint** and the phase has scheduled **security reviews** (Workstream Q) — a workstream isn't done until its gate passes. A proposed sequencing order is at the bottom; the founder re-prioritizes as we learn.

**Status legend** (same as `ROADMAP.md`): `[ ]` not started · `[x]` done · `[~]` in progress · `[!]` issue · `[d]` delayed — with a one-line note under any `[!]`/`[d]`.

**Ground rules carried over — these do not expire with Phase 1:**
- All `CLAUDE.md` hard constraints still apply: modular monolith, REST only, Stripe only, eSign via `IeSignProvider`, append-only events, signed-contract immutability, types live in `src/types/index.ts`.
- The four marketplace seams stay intact: typed accounts, event log, provider adapters, generic billing. Workstream X exists to *exercise* those seams, not bypass them.
- One feature fully working beats several half-done. Commit after each working feature.

---

## Workstream 0 — Phase 1 Closeout & Code Cleanup

*Goal: start Phase 2 from a verifiably clean base. Baseline measured 2026-07-15: working tree clean, all Phase 1 work committed & pushed, `npm run typecheck` passes with zero errors, `npm audit` reports zero vulnerabilities.*

- [~] Post-demo cleanup pass over `src/` and `public/`: remove dead code and unused exports, resolve stray TODOs, confirm every service follows the `AccountService`/`ContractService` pattern, consistent `ServiceError` usage across routes
  - **Audited 2026-08-17 — smaller than it reads, and now precisely scoped:**
    - [x] **Stray TODOs: none.** Zero `TODO`/`FIXME`/`XXX`/`HACK` in `src/`. Nothing to resolve.
    - [x] **`public/` removed.** The repo-root `public/` was **empty** and referenced by nothing (the app moved to `web/` in M0-b; Express serves `dist-web`). It was untracked by git, so this was a local phantom rather than committed dead code.
    - [ ] **8 raw `throw new Error(...)` bypass `ServiceError`** — `EventService.ts` (4) and `NotificationService.ts` (4). These surface as uncoded 500s instead of `{ code, message }`, so callers and the e2e suite can't key off `error.code`. Small, mechanical fix; **changes runtime error shapes**, so it wants its own commit rather than riding along with M0.5.
    - [ ] **Unused code is not compiler-enforced**: `tsconfig.json` sets `strict` but **not** `noUnusedLocals`/`noUnusedParameters`, so dead locals pass silently. Enabling both is the durable fix (turn on, fix the fallout, keep it honest thereafter). Unused *exports* need a tool (`knip`/`ts-prune`) — not currently a dependency.
    - Service-pattern conformance: all nine domain services follow the `AccountService`/`ContractService` shape (class + singleton export, Supabase access inside, `ServiceError` for failures). `contractDocument.ts`, `passwordPolicy.ts`, `imageUpload.ts`, `errors.ts` are helpers, not domain services — correctly exempt.
- [x] Repo hygiene: `PHASE_1_SUMMARY.md` still describes the abandoned stack (custom JWT, S3, Heroku) — archive or rewrite it so no doc contradicts the as-built system; refresh `ARCHITECTURE.md` to match what actually shipped — **DONE 2026-08-17.**
  - **`ARCHITECTURE.md` never existed.** It was never committed in this repo's history — the name came from the lost original scaffold package, yet `CLAUDE.md` has been pointing every session at it as a reference file. So this was *write*, not *refresh*. Now written from the shipped code: layering and directory map, the four marketplace seams, the auth/permission chain (incl. `requireCompleteProfile` and session refresh), security posture (CSP, CORS incl. native origins, the corrected limiter scoping, RLS, password policy), domain invariants (immutable signed contracts, append-only events, exactly-once auto-invoicing, webhook idempotency), the channel-agnostic notification queue, front-end/mobile build seams, environments/testing, and deliberate non-goals.
  - **`PHASE_1_SUMMARY.md` archived, not rewritten** — it is the summary of the *original scaffold plan* (it references `/home/claude/petpro-connect/` and a `README.md`/`IMPLEMENTATION_GUIDE.md` that never existed here), so rewriting 478 lines of an abandoned plan would be waste. It now opens with a prominent **ARCHIVED** banner plus a contradiction table (JWT→Supabase Auth, S3→Supabase Storage, Heroku→Render, Firebase→unused, "10 migrations"→25) and points readers to `ARCHITECTURE.md` / `CLAUDE.md` / the roadmaps. Kept as a record of the original requirements thinking.
  - Net effect: no doc contradicts the as-built system, and `CLAUDE.md`'s reference list now resolves.
- [ ] Decide and apply dependency upgrades (deliberate, one at a time, `npm test` green after each): Express 4→5, Zod 3→4, TypeScript 5→7, `@types/node` 20→current. None are urgent (zero vulnerabilities today) — this is about not accumulating a scary jump later
  - **Surveyed 2026-08-17 (`npm outdated` + `npm audit`): still 0 vulnerabilities.** The list splits cleanly, and the two halves deserve different treatment:
    - **Safe, same-major — can go in one batch:** `@supabase/supabase-js` 2.110→2.112, `pg` 8.22→8.23, `@types/pg` 8.20→8.23, `stripe` 22.3→22.5, `express-rate-limit` 8.6.1→8.6.2, `tsx` 4.23.1→4.23.12, `vite` 8.2.0→8.2.2.
    - **Breaking majors — strictly one at a time, `npm test` green between each:** `express` 4.22→**5.2** (+ `@types/express` 4→5, the riskiest: error-handling and routing changes touch every route), `zod` 3.25→**4.4** (validation lives on every endpoint), `typescript` 5.9→**7.0**, `@types/node` 20→**26**.
  - ⚠️ **Sequencing:** do **not** start this on top of the current uncommitted M0.5 tree. A major-version bump that breaks something must be bisectable against a known-green baseline, and mixing it into an unreviewed security diff makes both unreviewable and unrevertable. **Commit M0.5 first**, then take the safe batch as one commit, then each major as its own.
- [x] Set up CI (GitHub Actions): `typecheck` + `npm test` on every push, so a broken commit can't sit unnoticed — **DONE 2026-08-01** (`.github/workflows/ci.yml`: Node 24, applies migrations to the test project, boots the server, runs the full e2e; on push **and** PR). Surfaced that the app requires Node ≥22 (supabase-js Realtime needs a global WebSocket) — `engines` corrected accordingly.
  - ✅ Test-data decision made (2026-07-19, revised): **separate Supabase test project** — CI/dev point `npm test` at the test project via one env var, never prod.
- [x] Stand up a **separate Supabase test project** (**DECIDED 2026-07-19, revised: test project, not a live cleanup script**): Phase 1 test runs have been seeding accounts/clients into the production database — the fix is to stop pointing tests at prod at all. Keep migrations applied to the test project too (one extra target; they're applied by hand already), so it doubles as the rehearsal surface for schema changes before they hit prod. A live cleanup script was rejected because the append-only `events` log + no-hard-delete rule make it permanently leaky (it can deactivate/scrub but never remove test rows from prod). A convenience cleanup routine can still live inside the test project. **Setup + migration workflow written up in `TEST_PROJECT_AND_MIGRATIONS.md`.** — **DONE 2026-08-01**: local `.env` points at test project `dzejaycyjjaqajmatmjs` (all 23 migrations applied); `npm run dev`/`npm test`/`npm run migrate` hit it, closing the tests-write-to-prod leak. Prod (`tlfcilvsycjidmyrpsxi`) is reached only via gitignored `.env.prod`.
- [ ] **QA checkpoint 0:** full `npm test` + all weekly scripts green against Render after the cleanup lands — proves the cleanup changed structure, not behavior

## Workstream Q — QA Checkpoints & Security Reviews (recurring, gates the other workstreams)

*QA checkpoints are listed inside each workstream. The security reviews below are their own scheduled events, not afterthoughts. Findings from either get logged as `[!]` items in the owning workstream.*

- [~] **Security review #1 — largely complete 2026-08-01; 2 dashboard-only items remain (pairs with Workstream 0):**
  - [x] Tenant-isolation audit **DONE**: audited every route + service — app-code scoping by `professional_account_id` was already correct throughout (ClientService/PaymentService/portal/messaging; ownership-checked mutations; PII-free pay-token projection). RLS added as the second net: migration `023_enable_rls_all_tables` now applied to **both test and prod** (26/26 public tables) — deny-all against the anon PostgREST API, which the service role bypasses. (**DECIDED 2026-07-19: approved.**)
  - [x] HTTP hardening **DONE** (`src/api/middleware/security.ts`): `helmet` with a CSP tuned to the frontend + Supabase Realtime (browser-verified, no console violations), rate limiting on auth + portal-login + public pay-link + webhook, explicit default-deny CORS.
  - [~] Secrets review: **DONE** — nothing sensitive committed (verified against git history); key-rotation drill written (`KEY_ROTATION.md`); **prod DB password rotated 2026-08-01** and verified. **OPEN:** rotate the exposed prod `SUPABASE_SERVICE_KEY` (higher impact — logs out all sessions, so scheduled); confirm Render env vars are least-privilege.
  - [x] Input-validation sweep **DONE**: every route body already goes through Zod; list-endpoint query params (`status` enums, `client_id` UUID) now validated instead of cast as untyped strings.
- [x] Add **cross-tenant regression tests** to `npm test`: professional B attempting to read/modify professional A's clients, contracts, invoices, appointments must get 404/403 — this makes the tenant audit permanent instead of one-time — **DONE 2026-08-01** (e2e **step 15**: a second professional gets 404/403 on another's clients/pets/invoices across read/edit/delete, sees an empty list, and cannot tamper).
- [x] Standing rule from here on: **every new endpoint ships with a tenant-scoping test** in the same commit — **adopted 2026-08-01**.
- [ ] **Security review #2 — after Workstream X + the owner portal are live:** re-run the tenant audit with two account types (owner vs. professional) reading shared tables; verify magic-link auth can't reach professional-only surfaces; re-check webhook signature verification and idempotency under the expanded event set
- [ ] Device/browser QA matrix (used by checkpoints M and U): iPhone Safari, Android Chrome, desktop Chrome/Edge/Firefox — light + dark mode

## Workstream D — Account Lifecycle & Data Privacy (delete / deactivate)

> **Added 2026-07-19 at founder direction — data privacy and deletion is a first-class concern, not a backlog afterthought.** Detailed spec stays in `ROADMAP.md` → **P2-15**; this workstream schedules the build and its gates. It lands **early** because it's the seam **M-Connect's** Stripe Connect account depends on. (Test-data residue is handled separately by Workstream 0's test project — **not** by this; the earlier coupling was dropped when the test-data decision flipped.)

**Core principle (from the spec — do not violate):** a hard row delete is architecturally impossible *by design* — four FKs reference `accounts(id)` with no `ON DELETE` rule, and the `events` append-only trigger (Marketplace Seam 2 + a `CLAUDE.md` hard constraint) forbids deleting event rows. **Build it as deactivation + PII scrub, never row deletion.**

- [x] **Deactivation core:** set `status = 'deactivated'` (the enum value has existed unused since migration `001`); block login and reject the session in `requireAuth`. This is the seam — a reversible status flip, not a destructive op. — **DONE 2026-08-01** (`AccountService.deactivateAccount()`). `requireAuth` already 403'd any non-active account, so every live session dies on the flip; also added a status guard to `authenticateProfessional` so re-login is refused. Migration `024` adds `accounts.deactivated_at`.
- [x] **PII scrub:** null/redact the identifying columns on `accounts` (`email`, `phone`) so a "delete my data" request is satisfied while the immutable history keeps referring to a row that still exists (keep `id`). — **DONE 2026-08-01**, scope = **contact-only** (founder decision 2026-08-01, chosen over deep/minimal): scrub the *leaver's own* contact identifiers — `accounts.email` → unique tombstone (`deactivated+<id>@deleted.invalid`, since it's UNIQUE NOT NULL and can't be nulled), `accounts.phone` → null, plus the professional's `bio` / the owner's `address`. **Client records are NOT touched** — this honors the retain-clients decision below (retained clients are separate people with live portal access; wiping their `general_notes`/gate codes would degrade records the professional must keep). `professional_profiles.business_name` **and** `full_name` are kept so the portal can still name the walker as "deactivated".
- [x] **Supabase Auth user:** delete or disable the matching `auth.users` row, or a deactivated account can still authenticate (`AccountService.ts:61` already flags the orphan-auth-user case — this seam is known fiddly). — **DONE 2026-08-01**: deletes the auth user then nulls `accounts.auth_user_id`. Best-effort (logged on failure) because the status flip is the authoritative lockout; the deleted user also means dead tokens 401 at `getUser` before the status check even runs.
- [x] **Retention guardrails:** signed contracts (`generated_html` immutable once signed), invoices, and transactions must survive deactivation untouched — retention/legal value, and a hard constraint. — **DONE 2026-08-01**: the method never references those tables; e2e step 16 asserts the signed contract (status + `generated_html` intact) and invoice remain readable after deactivation.
- [x] **Connected-account link (M-Connect seam):** the Stripe Connect `account_id` is a **nullable, replaceable** link cleared on deactivation / relationship-termination — never hardwired into the payment path. M-Connect builds on this; deactivation must null it cleanly. — **DONE 2026-08-01**: migration `024` adds nullable `accounts.stripe_connect_account_id` (nothing populates it until Workstream M); deactivation nulls it; e2e plants a dummy id and asserts it's cleared.
- [x] **e2e cleanup path (convenience, not load-bearing):** deactivation gives the test suite a tidy way to retire accounts *within the test project*. — **DONE**: `accountService.deactivateAccount(id)` is idempotent and callable directly, so tests/cleanup can retire accounts.
- [x] **Client handling (DECIDED 2026-07-19 — retain, portal access unaffected):** a deactivated professional's client rows and their owners' portal login stay intact (orphan-but-retain — real people with signed agreements). Wherever the walker is surfaced to an owner, they **display as "deactivated"** rather than disappearing. — **DONE 2026-08-01**: `PortalService.overview()` returns a `professional.deactivated` flag (withholding the scrubbed email/phone, keeping business name); `portal.js` renders a "Deactivated — account closed" card instead of the contact details. Client rows are untouched; e2e asserts the owner still sees their retained client + the deactivated walker.
- [x] **QA checkpoint D:** deactivate a professional **and** an owner account → login blocked, session rejected, PII scrubbed, auth user gone → signed contracts / invoices / transactions still intact and readable → the professional's clients retained, their owners' portal login still works, and the walker shows as "deactivated" in the portal. — **DONE 2026-08-01** as **e2e step 16** (green locally; runs on push/PR in CI). Self-service surfaces: `POST /api/auth/deactivate` (professional, password-confirmed) + "Close my account" in the Profile tab; `POST /api/portal/deactivate` (owner, passwordless — the magic-link session is the proof).

## Workstream M — Mobile: Android-first native app

> **Founder direction 2026-07-19: build a real native app, Android first.** This supersedes the earlier PWA-first stance (a good mobile web app was going to come first, native only when a feature demanded it). The founder's original goal was an app for iOS **and** Android; the plan below gets there in phases. Phase 1 (the web app) is unchanged and remains the demo surface for now. iOS follows in **Phase 3** (below).

**Approach:** Capacitor wraps the existing web app (`public/`) into an Android shell — still one deployable, same backend. Native device features are Capacitor plugins called from the existing UI, pulled forward from the backlog.

**Why Android-first:**
- Android builds on the founder's **Windows** machine — no Mac needed. That constraint moves to Phase 3 with iOS.
- Google Play does **not** apply Apple's 4.2 "not just a website" wrapper rejection, so native features here are driven by walker value, not review-clearing. (Apple's 4.2 gauntlet re-enters in Phase 3.)

### Native features pulled forward (Phase 2, Android)
- [ ] **QR / barcode check-in — with manual fallback** (P2-2, decided 2026-07-19): scan the dog's tag to start/end a visit; a "Start walk" button **always** works with no tag. QR is a convenience the walker *may* use, never a dependency (candidate for a paid tier — see gating). App generates a per-pet QR to print/attach.
- [ ] **GPS walk tracking — starts on walk start** (P2-2, founder note 2026-07-19): live route + distance, running **only** from "Start walk" to "Complete walk" — no always-on/background location. Implemented as an Android **foreground service** with an ongoing "Walk in progress" notification during the walk only (permission-friendly and battery-friendly vs. always-on).
- [ ] **Tap-to-Pay (Android)** (P2-8, pulled into Phase 2 for testing 2026-07-19): Stripe Terminal SDK, connection-token endpoint, in-person card flow. Needs an **NFC + Android 11+** device Stripe supports. Routes to the walker's connected account once **M-Connect** is done; on the platform account for founder-only testing before then. Independent of QR/GPS — can be resequenced ahead of M1 if testing payments early matters, provided the security gate (M0.5) is done first.
- [ ] **Push notifications**: native channel extending Week 7's `notification_queue` (reminders, walk-done, new message, payment).
  - **Founder confirmed as wanted 2026-08-17**, after seeing notifications arrive only by email on the first device test: *"Walkers should have the option to receive notifications on their phones."* ✅ Already planned — noting the explicit ask so it isn't re-derived, and recording that it should be **opt-in per walker** ("the option to"), i.e. a per-account channel preference rather than a forced switch from email.
  - **No re-architecture needed:** Week 7 deliberately made every notification a `notification_queue` row (template + recipient + **channel**) behind an `IEmailProvider`-style adapter, precisely so a second channel drops in. Push = a new adapter + `channel: 'push'` + device-token registration; senders/callers don't change. The device-token store is the only new schema.
- [ ] **Camera / photos** (P2-3; also feeds P2-7 + P2-11, now Phase 3): dog photo on the walk report. The Capacitor camera plugin is Phase 2 infra; the pet-photo and profile/logo *UIs* it serves moved to Phase 3 with P2-11/P2-7. **The same `@capacitor/camera` plugin is the fix for M0-IMG (HEIC upload failure) below — pulled forward to M0 close-out for the profile/logo upload that is live today.**
- [ ] **Calendar integration** (P2-4): push walks to the phone's calendar.
- [ ] **Local notifications + "walk in progress" home-screen widget.**

### M-Connect — Payment setup: "Get paid" (Stripe Connect / P2-6)
*Gap found 2026-07-19: the simplified onboarding has **no way for a professional to set up getting paid** — today all money routes to the platform (founder) Stripe account. Real walkers can't receive money until this exists, and Tap-to-Pay to a real walker's account depends on it.*
- [ ] **Stripe Connect Express** (founder decision 2026-07-19) — Stripe-hosted KYC/onboarding.
  - ⚠️ **Do not hardwire the connected account.** It must be disconnectable/changeable later — e.g. if the professional relationship is terminated — so build it behind the same seam as **Workstream D** (P2-15) account deactivation, not as a permanent one-time link. Storing the `account_id` is a nullable, replaceable link on the account, never an assumption baked into the payment path.
- [ ] Generate onboarding link, store connected `account_id`, handle the incomplete-onboarding (not-payout-ready) state.
- [ ] Route charges (Checkout **and** Tap-to-Pay) to the walker's connected account. `PaymentService` was built so Connect slots in without changing callers.
- [ ] "Set up payments" step in the onboarding wizard (skippable) **plus** a persistent Profile card until complete.
  - **Founder confirmed 2026-08-08:** add this as an **optional** step in the signup process so a professional can set up accepting payments during onboarding (and skip to do it later). ✅ Already the plan — noting the explicit ask so it isn't re-derived.
  - ⚠️ **We never collect raw banking details ourselves.** With Stripe Connect Express (line above), bank account / tax ID / KYC are entered on **Stripe's hosted onboarding**, not a form in our app — we only store the returned `account_id`. This keeps card/bank credentials off our server (same principle as Checkout) and is the safe, correct way to satisfy "add banking information."
- [ ] **Not mobile-specific** — build on web, the Android app inherits it. Slots **before** Tap-to-Pay (M2). Founder-only testing can defer it (platform-account fallback); real-walker payment testing needs it first.

### Foundation & polish (folded in from the original Workstream M)
- [~] Responsive audit at 375px: navigation, tables/cards, forms, the schedule week view (7 columns won't fit → mobile day/agenda layout), modals and toasts.
  - Note (2026-08-10): **audited in code + live at 375px — largely already done.** The schedule week-view worry is **moot**: `renderSchedule` (`web/src/app.js`) already emits a stacked day-header + appointment-card **agenda**, not a 7-column grid. Nav collapses to a fixed **bottom bar** on phones (#8, `styles.css` @620px, safe-area inset). Form grids → 1 column @620px, sign layout → 1 column @760px. Login/signup measured clean at 375px (no horizontal overflow; inputs 44px tall). No `<table>` elements to overflow. **Remaining = verification, not build:** a live pass on *authenticated* screens (needs a real login) and a real-phone check. Founder-owned.
  - Update (2026-08-17): **both remaining conditions were met** — the founder installed the APK, logged in, and clicked through every screen on a real phone. No layout breakage reported; the feedback that came back was *aesthetic* (**U-M1**), which is a Workstream U concern, not a responsive-layout failure. **Ready to mark `[x]` at founder sign-off** — left unticked because the verification is founder-owned.
- [~] Touch-first pass: tap-target sizes, the signature pad under touch (test on a real phone — pointer events differ from mouse), mobile date/time pickers.
  - Note (2026-08-10): **largely already done in code.** Signature canvas uses `touch-action: none` (finger-drawable). Date/time use **native** `type="date"`/`type="datetime-local"` inputs → correct mobile OS pickers for free; `type="email"`/`type="number"` trigger the right mobile keyboards. Buttons ~42–44px tap height. **Remaining = a real-phone test** (signature pad under actual touch, one-handed reachability) — needs the physical Android device, founder-owned.
  - Update (2026-08-17): the app is now installed and navigable on the phone, but **the specific outstanding item — signing a contract with a finger — is still unverified.** Tap targets and navigation were exercised; the signature canvas was not. Stays `[~]` until someone actually draws on it. (Note the M0-IMG/M0-GATE interaction: reaching the sign screen needs a completed profile, so the photo workaround may be required first.)
- [x] PWA baseline: manifest + icons, service worker with a safe cache strategy (never cache API responses that would show stale schedules), graceful offline message. (Week 7's offline draft-sync endpoint is the seam — don't rebuild it, just don't break it.)
  - DONE 2026-08-10 (commit `22dc192`, shipped in #4): `manifest.webmanifest` + icon, `sw.js` with a safe cache strategy, `pwa.js` registration.
- [x] **Vite + TypeScript build step** (**DECIDED 2026-07-19: approved** — the Workstream U modernization path, now confirmed): Capacitor expects a web build pipeline; bolting plugins onto the single 2,742-line `app.js` is fragile.
  - DONE 2026-08-10 (commits `c743674` build pipeline + `d3040b3` ES-module refactor, shipped in #4): `web/` split into `src/` modules with an API base seam; `npm run build` runs `tsc && vite build`.
- [ ] One-handed field workflow: check schedule / mark complete / collect payment each ≤ 2 taps from home (builds on Phase 1 finding W-4).
  - Note (2026-08-10): not started — no evidence in the shipped M0 work. Carry into M0 close-out.
- [~] **Security gate before payments face public installs** (pairs with Security review #1): `helmet`, rate-limiting on auth + webhook endpoints, on-device auth-token storage review. — **All three named deliverables are DONE:** `helmet` + rate-limiting 2026-08-01 in Security review #1 (limiter scope corrected 2026-08-17, see **M0-RATELIMIT**), and the **on-device auth-token storage review 2026-08-17** (below). **Deliberately still `[~]`, not `[x]`:** one remediation from that review — installing the Keystore-backed secure-storage plugin (T-3) — is not shipped, and this item is the gate for *public installs*. Do not read it as closed until that lands.
  - [x] **Onboarding-gate hardening — folded into M0.5 (2026-08-17, from M0-GATE below). DONE 2026-08-17.**
    1. [x] **Server-side gate** — `requireCompleteProfile` (`src/api/middleware/auth.ts`) mirrors the client's `setupDone()` and 403s `profile_incomplete`. Applied to the create/issue paths only: `POST /api/contracts`, `PATCH /api/contracts/:id`, `POST /api/contracts/:id/sign`, `POST /api/contracts/:id/send`, `POST /api/billable-items`, `POST /api/invoices`, `POST /api/invoices/:id/checkout`, `POST /api/invoices/:id/send`. **Reads stay open** (the client-detail screen fetches contracts + invoices unconditionally — gating GETs would break the page for un-onboarded users), and so do **void/sync** so nobody gets trapped mid-flow. Middleware layer, not service layer, so **walk-completion auto-invoicing and the billing-cadence worker are unaffected**.
    2. [x] **Client gate co-located; route-shape list retired** — the router's hand-maintained `gatedRoute` string-match is gone. `requireSetup()` now sits next to `setupDone()` and is called by `renderNewContract` / `renderSign` themselves, so a newly added contract route can't silently skip it. The checkout button needs no client guard: the server enforces it and the button only renders when onboarded.
    - **Verified:** `npm run typecheck`, `build:web`, `check:structure` green; **e2e 16/16 pass**, and step 1 now *proves the gate* — a freshly signed-up account gets 403 `profile_incomplete` on `POST /api/contracts` before completing onboarding. `ApiResult` gained `errorCode` so assertions key off the machine-readable code, not user-facing copy.
  - [x] **On-device auth-token storage review — DONE 2026-08-17.** Findings:
    - **Where the token lives:** `saveSession()` (`web/src/app.js`) puts the Supabase **access token** in `localStorage['petpro_token']` (portal: `petpro_portal_token`). `supabase-js` is created with `persistSession: false, autoRefreshToken: false`, so the app manages the token itself — supabase-js stores nothing.
    - [x] **T-1 (fixed) — backup exfiltration.** `android:allowBackup` was `"true"` (the Capacitor default), which sweeps the WebView's localStorage — token included — into **Google Auto Backup (the user's Drive)** and `adb backup`. Set to `"false"`, plus a new `res/xml/data_extraction_rules.xml` excluding everything from **cloud-backup *and* device-transfer** (on Android 12+/targetSdk 36, D2D transfer is governed by that file, *not* by `allowBackup`). Verified in the **merged** manifest, not just source.
    - [x] **T-2 (RESOLVED 2026-08-17 — refresh built) — `refresh_token` was returned but never used.** `AccountService.authenticateProfessional` returns `refresh_token` + `expires_at` to the browser (`AuthSession`), but **no refresh endpoint exists anywhere in the API** and the client's `saveSession()` reads only `access_token`. So a long-lived credential crosses the wire and is discarded. Consequence: with the Supabase default ~1h access-token TTL and no refresh, **a walker is logged out roughly hourly in the field.** Either stop returning it, or build refresh properly — see the decision below. **Resolved by building refresh:** `refresh_token` is now load-bearing rather than dead weight, persisted through the token seam and rotated on every use.
    - [~] **T-3 (decided 2026-08-17; seam built, plugin pending) — token at rest in WebView localStorage.** XSS exfiltration is already well-mitigated: helmet's CSP keeps `script-src 'self'` and `script-src-attr 'none'` (no inline handlers), and M0-b bundled supabase-js so there's no CDN allowance. Residual risk is **at-rest extraction** on a rooted/forensically-imaged device — now the *only* remaining path, since T-1 closed backup. Fix if wanted: a Keystore-backed secure-storage plugin. Note `@capacitor/preferences` is **not** secure storage (plain SharedPreferences) and would be no better than today. **Status:** the `tokenStore` seam that makes the swap a one-file change is built and shipping; only the plugin install remains (see the Keystore-plugin item below). Until it lands, tokens are still in localStorage — better than before (backup exfiltration closed by T-1) but not Keystore-protected.
    - ⚠️ **T-2 and T-3 interact:** implementing refresh means *storing a long-lived refresh token on the device*, which raises the bar for T-3. Decide them together.
    - ✅ **DECIDED 2026-08-17 (founder): do both — session refresh AND Keystore-backed secure storage**, as one job, since the refresh token must never land in localStorage.
    - **BUILT 2026-08-17 — steps 1–3 done, step 4 revised, plugin deliberately sequenced last:**
      - [x] **`POST /api/auth/refresh`** (`AccountService.refreshSession` + route). Re-checks deactivation on every refresh — otherwise a refresh would quietly resurrect a session for an account closed after login — and returns the same generic message as a bad login so it can't probe account state. ~~Inherits the `/api/auth` limiter (20/15min), the right bucket for a credential exchange.~~ — **that was wrong, and it caused a real lockout the same day.** Refresh is called routinely, so sharing the credential budget meant a spent budget broke refresh → forced logout → throttled login. It now has its own `refreshLimiter` (60/15min); see **M0-RATELIMIT**.
      - [x] **Storage seam** (`web/src/tokenStore.js`) — **async API by design**: localStorage is synchronous, but every Capacitor secure-storage plugin is promise-based, so writing callers against a sync API would mean rewriting every call site later. Async now ⇒ the plugin is a change to *one file*. Only **secrets** go through it; cached account/profile/drafts stay in plain localStorage (not credentials, and Keystore storage is slower/size-limited). Boot now restores the session before the first render.
      - [x] **Transparent single-flight refresh** in `api()` — on 401 it refreshes once and retries, logging out only if the refresh itself fails. **Single-flight matters more than it looks:** Supabase *rotates* the refresh token, so parallel refreshes would race and the first to land would invalidate the others, turning a recoverable expiry into a logout.
      - [x] **e2e coverage** (step 1): refresh returns a working access token, the refresh token **rotates**, and a **chained** second refresh still works — that chain is the loop the app runs all day. Deliberately *not* asserting that replaying a spent token 401s: Supabase has a ~10s **refresh-token reuse interval** that intentionally returns the same session for retried/concurrent calls, so that assertion is a race (it failed exactly that way on first run). 16/16 green.
      - [~] **Portal refresh — revised to "not now", with reason.** The portal is a **browser-only** surface (the native shell loads `index.html`; there's no route into `/portal`), so there is no secure storage to protect a long-lived owner token — refresh there would mean parking a long-lived credential in plain localStorage. Combined with owners using the portal briefly and rarely, and it showing contracts + payment links on what is often a shared home device, **shorter sessions are the better trade**. Revisit only if owners complain.
      - [ ] **Keystore plugin — chosen, NOT yet installed (deliberate).** Recommended: **`@aparajita/capacitor-secure-storage`** — MIT, v**8.0.0** published 2026-02-10 tracking the Capacitor 8 major, steady cadence (7.1.2→7.1.6→8.0.0), ~93 KB, Android Keystore/EncryptedSharedPreferences + iOS Keychain (so Phase 3 inherits it). Alternative `capacitor-secure-storage-plugin` (0.13.0) declares `@capacitor/core >=8` but is still 0.x after years. **Sequenced after the founder's first successful install**: the native path can't be verified without the physical device, and shipping an unverified native plugin into the very first install would confound any failure there. The seam means adding it later costs one file, not a refactor.
    - Original build order for reference:
      1. **Server:** `POST /api/auth/refresh` — exchange a refresh token for a new session via Supabase `refreshSession`; rate-limited like the other auth routes. Keep `refresh_token` in `AuthSession` (it becomes load-bearing rather than dead weight).
      2. **Client token store behind one seam:** a small `tokenStore` module — **native → Keystore-backed secure storage, web → localStorage** — so `saveSession`/`logout`/boot all go through one place instead of touching `localStorage` directly. Do the seam *before* the plugin so the web build never regresses. ⚠️ `@capacitor/preferences` is **not** secure storage; needs a Keystore/EncryptedSharedPreferences-backed plugin (plugin choice is a new third-party dependency — flag it before adding).
      3. **Transparent refresh:** the `api()` wrapper currently logs out on any 401. Refresh once on 401 and retry the request, logging out only if the refresh itself fails — that's what removes the hourly re-login. Must be single-flight so concurrent 401s don't fire N refreshes.
      4. **Portal too** (`petpro_portal_token`) — magic-link sessions have the same ~1h expiry, and the roadmap's "portal asks for a fresh link" note is the same root cause.
  - **Incidental unblock (2026-08-17):** the Android project no longer evaluated under **AGP 9.3.1** — the Capacitor template's `getDefaultProguardFile('proguard-android.txt')` was removed in AGP 9, failing project evaluation before any build task. Switched to `proguard-android-optimize.txt` (`android/app/build.gradle`, the documented replacement; `minifyEnabled` is false so it only has to resolve). See **M0-BUILD** below — this was blocking every Android build.
- [x] **M0-BUILD — the Android app had never actually been built or installed; the AGP 9 bump broke it silently** *(found + fixed 2026-08-17)*
  - **What was true:** `com.petpro.connect` was **never installed on the founder's phone**. All "M0 device testing" to date (incl. the M0-IMG photo failure and the M0-GATE bottom-nav escape) was done in **mobile Chrome against the Render URL**, not in the native shell. So **M0's exit criterion — "existing app in the native shell on a real device" — was not met**, independent of the other findings.
  - **Why it went unnoticed:** the Aug 1 Capacitor scaffold (`4703f11`) used **AGP 8.13.0**, where the generated `proguard-android.txt` line is valid; Android Studio synced fine that day, which is what "build env complete 2026-08-01" recorded. The **2026-08-10 bump to AGP 9.3.1** (`a968822`, shipped in M0 PR #4) left that line untouched, and AGP 9 rejects it. Because `proguardFiles getDefaultProguardFile(...)` is evaluated during Gradle's **configuration phase**, the failure hits *every* invocation — including debug builds and Android Studio's Gradle **sync** — not just release. It therefore presented as "sync failed," never as a message about ProGuard or release builds.
  - **Fixed + verified 2026-08-17:** with the one-line ProGuard change, `gradlew assembleDebug` completes — **first successful APK: `android/app/build/outputs/apk/debug/app-debug.apk` (4.1 MB)**, debug-keystore signed, installable by sideload.
  - ⚠️ **Consequence for M0-IMG:** the HEIC failure was observed in **mobile Chrome**, not the native shell. The root cause is the same either way (the WebView/browser can't decode HEIC in the `<img>`→`<canvas>` downscale step), but the planned `@capacitor/camera` fix **only covers the native app** — the browser path keeps the limitation, so the web fallback error message stays load-bearing. Re-test M0-IMG on the installed APK before closing it.
- [x] **M0-APIBASE — first two APKs shipped with no backend URL ("Request failed 200")** *(caused + fixed 2026-08-17)*
  - `MOBILE.md` documents that native builds must run with `VITE_API_BASE` set; Vite deliberately does **not** read it from `.env`. Two APKs were built without it, so `API_BASE` compiled to `''`. Empty is *correct* for the web build (same-origin), but in the native shell the app called `https://localhost/api/...`, which the WebView's own asset server answered with `index.html` and **HTTP 200** — so the failure surfaced as `Request failed (200)` rather than a network error.
  - **Fixed:** rebuilt with the variable and verified the origin **inside the APK archive**, not just in source.
  - **Made unrepeatable:** `build:native` now runs `scripts/check-native-env.mjs`, which fails the build when `VITE_API_BASE` is unset *or* points at localhost/127.0.0.1 (which on a real device is the phone itself). Verified it blocks a bare build and passes with the variable set. A silent broken APK is now a loud build failure.
- [x] **M0-RATELIMIT — ordinary use locked the walker out of logging in** *(found on-device + fixed 2026-08-17)*
  - **Symptom:** after clicking through the app (and retrying the photo upload several times), login stopped working.
  - **Cause:** `app.use('/api/auth', authLimiter, …)` put the **whole** auth router in one 20-per-15-minute credential bucket — including `GET /api/auth/me` (fires on **every Profile view**), `POST /api/auth/profile/image` (every upload retry), profile saves, and M0.5's new `/api/auth/refresh`. Normal use exhausted the budget, and then **login itself was throttled**. M0.5 made it worse: an expired token → refresh → 429 → refresh fails → forced logout → login also 429. `trust proxy` is set, so this was per-device, not a shared bucket.
  - **Fixed:** the credential limiter now applies only where guessing is the threat (login, signup, forgot/reset/change password, deactivate). Refresh gets its own `refreshLimiter` (60/15min) — frequent but unguessable. Everything else under `/api/auth` is already behind `requireAuth`. Typecheck + e2e 16/16 green.
  - ⚠️ **Server-side fix — requires a deploy to Render to take effect.** No APK rebuild needed.
- [ ] **U-M1 — the app "looks outdated; does not have a modern, light feel"** *(founder, first native device test 2026-08-17)* — belongs to **Workstream U**, logged here because it came out of M0 testing. The Phase 1 UI was built to be demoable, not delightful (see Workstream U intro). This is the first look at it as a *native app*, where the bar is other phone apps rather than a web demo. Treat as the founder's headline UX signal for U — not a bug list.
- [ ] **U-M2 — signed/generated contract renders on a white background**, jarring against the app *(founder 2026-08-17)*. Note this is currently *by design*: the contract document is standalone **print-styled** HTML (the same rendering the signed email attaches and that Print → Save as PDF produces), so it deliberately looks like paper and does not follow the app's theme. Decide deliberately in U: keep paper-white for fidelity to the printed artifact, or theme the in-app *viewer* while keeping the print/email copy white. Don't "fix" it without that call — the email attachment and PDF path share the rendering.
- [!] **M0-IMG — profile/photo upload fails for phone photos (HEIC/HEIF): blocks onboarding on device** *(found 2026-08-17, M0 device testing)*
  - **Symptom:** on the Android app, uploading a profile photo taken with the phone camera errors with "the image has to be png or jpg." The profile photo is a **required step to finish signup** (`web/src/app.js` onboarding gate — step 1 hard-blocks with "Please add a profile photo to continue"), so a walker whose phone saves in HEIC **cannot complete onboarding on the device.** This is an M0 close-out blocker, not cosmetic polish.
  - **Root cause:** `downscaleImage` (`web/src/app.js`) loads the picked file into an `<img>` → `<canvas>` to shrink and re-encode it. The Android WebView (and iOS WKWebView) **cannot reliably decode HEIC/HEIF in an `<img>`**, so `img.onerror` fires the "try a PNG or JPEG" message before anything reaches the server. Modern phones default to HEIC (Samsung "High efficiency," Pixel; **every iPhone since iOS 11** — so this is *worse* on the Phase 3 iOS port). The `<input accept="…">` filter is only advisory on Android, so a HEIC file still comes through the picker. The server (`src/services/imageUpload.ts`) also only accepts PNG/JPEG/WebP by magic bytes — but the request never gets that far.
  - **Fix — normalize to JPEG at the native layer, both platforms (`@capacitor/camera`):** on native, route the image field's Upload button through `@capacitor/camera` `getPhoto()` (source = Prompt → "Take Photo" / "Choose from Library") instead of the raw `<input type=file>`. The plugin returns a **JPEG** data URL on **both Android and iOS** — it converts HEIC → JPEG in native code, so the fragile WebView canvas-decode step never sees HEIC. That JPEG flows unchanged into the existing `POST /api/auth/profile/image` (**no server change** — it already accepts JPEG). Keep the `<input type=file>` path on web behind a `Capacitor.isNativePlatform()` check (desktop HEIC is rare; the current error message stays as the web fallback).
  - **Config this needs — do both now so the Phase 3 iOS port inherits it:**
    - **Android:** add `@capacitor/camera`; the Android 13+ Photo Picker needs no gallery permission, but declare `CAMERA` (and `READ_MEDIA_IMAGES` for older devices) in `AndroidManifest.xml` for the "Take Photo" path.
    - **iOS (Phase 3):** add the `Info.plist` usage strings — `NSCameraUsageDescription`, `NSPhotoLibraryUsageDescription`, `NSPhotoLibraryAddUsageDescription` — or the app hard-crashes on first camera/library access. Logged now so it is not rediscovered under Apple review.
  - **Rejected alternative:** server-side HEIC decode (`sharp` + libheif) — heavier native dependency, and it would not fix the client-side canvas step that also drives the on-screen preview. Native-plugin conversion is the smaller, correct fix.
  - **Scope note:** shares the `@capacitor/camera` plugin with the "Camera / photos" native feature (dog photo on walk reports) above — same infra, but this path is **live today** (profile + logo upload) and gates onboarding, so it lands with **M0 close-out**, ahead of the walk-report photo UI (Phase 3).
  - **Immediate test-session workaround (no code):** screenshot the photo and upload the screenshot (screenshots are PNG), or switch the phone camera to "Most compatible"/JPEG format.
- [x] **M0-GATE — onboarding is skippable, but contracts + payments stay locked until it's complete** *(founder decision + built 2026-08-17, from M0 device testing)* — **DONE (UI + server, hardened in M0.5 the same day).**
  - **Context:** during M0 testing the setup screen turned out to be escapable via the bottom nav (the "required photo" only blocked the Continue button). Founder call: **skipping setup is fine**, but a professional must not reach **contracts or payments** until onboarding is complete (`setupDone()` = full name + business name + phone + profile photo + ≥1 service type).
  - **Built (`web/src/app.js`):** a central **route guard** redirects `#/client/:id/new-contract` and `#/contract/:id/sign` to `#/setup/1` with a toast when `!setupDone()` — a catch-all for every link into those flows (Today cues, Edit terms, etc.). On the client-detail screen the entry points are also hidden: the **Generate contract** button is dropped and the **Contracts** + **Billing** sections show a "Finish setting up your profile" prompt instead of the create/invoice/collect-payment controls. Verified: `npm run build:web` + `check:structure` green; onboarded path renders byte-identical to before.
  - ~~⚠️ UI-layer only~~ — **closed 2026-08-17 in M0.5.** The API now refuses contract/invoice creation for an incomplete profile (`requireCompleteProfile`), and the client gate was co-located onto the capability entry points, retiring the hand-maintained route-shape list. See the "Onboarding-gate hardening" sub-item under the security gate above.
  - **Interaction with M0-IMG:** because the gate requires a profile photo, a HEIC-only phone that can't upload one (M0-IMG) also can't reach contracts/payments — the gate working as intended, but it means device-testing those flows needs the M0-IMG workaround (screenshot upload) until the camera-plugin fix lands.

### M-Brand — Logo + Android icon *(pulled forward from Phase 4, decided 2026-08-23)*

*Founder decision **D4-4**, 2026-08-23: **yes, pull it forward.** The launcher icon and splash are still the stock Capacitor/Android Studio defaults — `values/ic_launcher_background.xml` is `#FFFFFF`, `drawable-v24/ic_launcher_foreground.xml` is the default Android robot. **Google Play will not accept that build**, so this is a submission blocker sitting in a design phase that hasn't started. It also fixes the thing every test build looks like today.*

⚠️ **Specs live in `PHASE_4_ROADMAP.md`, not here.** This section is the **schedule**; the items are specified once, in Phase 4, under their permanent IDs. Don't re-describe the work here — cite the ID, and tick the box in **both** files when it lands. (Same stable-ID rule the `P2-x` items follow.)

**Not on the code critical path.** M-Brand is design work; it runs **in parallel** with M0 close-out / M0.5 / M-Connect rather than blocking them. The only hard ordering is that it must be **done before M-Play**.

- [ ] **B-1: Brand brief** (`docs/brand/BRIEF.md`) — **founder-authored, and it goes first.** One page: who it's for, three adjectives, what it must *not* look like, two or three reference products. Nothing gets drawn before this exists.
- [ ] **B-8: ⚠️ Trademark sanity check** — **do this before the mark is finalized, not after.** "PetPro" is a common construction in this market; a USPTO + app-store search is cheap now and expensive once the mark is on a Play listing and every client's invoice. Founder task, possibly with counsel. **Sequencing:** it can run concurrently with B-1/B-3 exploration, but **B-2's wordmark must not be finalized until it clears** — a conflict found late invalidates the icon, the splash, and the store listing together.
- [ ] **B-2: Name and mark lockups** — primary mark, wordmark, horizontal lockup (app header), stacked lockup (login, email, print). **Blocked on decision D4-6** — "PetPro" vs "PetPro Connect", i.e. which is the brand and which is the product. That decision was filed as non-urgent in Phase 4; pulling this work forward **promotes it to near-term**, because there is no wordmark to draw without it. Also worth settling how it relates to the verified sending domains (`eastwestoak.com`, `itchytail.com`) — a brand mismatch in the From line costs deliverability trust.
- [ ] **B-3: Design the mark** — the item N-1/N-2 actually block on. Non-negotiables from where it has to live: legible at **16px**, inside the **maskable safe zone** (center 66% — Android crops to circle/squircle/rounded-square by launcher), works in **one flat color** and reversed out of steel blue, distinct in light *and* dark, **SVG source of truth**. Explore at least one non-paw direction before defaulting to the paw (`web/public/icon.svg` is a placeholder paw today).
- [ ] **B-4: Palette v2** — pulled because the icon, splash, and `manifest.webmanifest` `theme_color`/`background_color` all need settled values, and picking them twice is waste. Keeps the four brand colors and the once-per-screen orange rule; adds the link/interactive color (retiring the six hardcoded `#7FB4CD`s), a neutral ramp, semantic status colors, and dark partners — **every pair contrast-checked to WCAG 2.1 AA** (a `SPEC.md` requirement).
- [ ] **N-1: Android adaptive icon** — foreground + background layers at every mipmap density, plus the **monochrome layer for Android 13+ themed icons**; verified in circle / squircle / rounded-square masks and against a busy wallpaper.
- [ ] **N-2: Splash screen** — replace the generated `splash.png` across all `drawable-port-*` / `drawable-land-*` densities, dark variant, and tune the Capacitor splash config so it doesn't outlast the app's boot.
- [ ] **N-3: PWA/web icon set** — maskable icon (`purpose: "maskable"`), PNG fallbacks at 192/512, apple-touch-icon, favicon; re-check `theme_color`/`background_color` against palette v2. **Today `manifest.webmanifest` ships a single `icon.svg` with `purpose: "any"`**, so Android letterboxes or crops it arbitrarily. Rides along because it's generated from the same source SVG in the same sitting.
- **Deliberately NOT pulled:** B-5 (typography — carries the open licensing decision **D4-3**), B-6 (iconography), B-7 (voice), B-9 (brand guide), N-4/N-5 (store listing creative + copy, which want real redesigned screens), N-6 (iOS assets, Phase 3).
  - ⚠️ **Accepted consequence:** the mark gets drawn **before** the brand typeface is chosen, so **B-2's wordmark and lockups may need a second pass** once B-5 lands. The **symbol** (B-3) — which is what the launcher icon, splash, and favicon actually use — is unaffected by the type decision, so the Play blocker clears either way. This was accepted knowingly when D4-4 was decided; don't re-litigate it as a mistake later.
- [ ] **QA checkpoint M-Brand:** every mark variant at 16 / 32 / 48 / 192 / 512px, light and dark, in circle / squircle / square masks, and in one flat color; icon + splash verified on **two real Android devices with different launchers** in both themes; `npm test` (incl. `check:structure`) green; founder sign-off. (Phase 4's QA checkpoints B and N inherit this evidence rather than repeating it.)

### Feature gating (forward reference — not built in Phase 2)
Pricing tiers (**Workstream S**) will later decide free vs. paid. Build every Phase 2 feature behind a **clean entitlement check** from the start so gating is a config change, not a refactor — same discipline already written into Workstream S. Premium-tier candidates to flag now: **QR check-in, GPS tracking, Tap-to-Pay, photo walk reports, calendar sync**.

### Phase estimates & time-to-test (Android)
| Phase | Scope | Build (wks) | Testable on Android at the end |
|---|---|---|---|
| **M0** Foundation | responsive + touch + PWA + Vite + Capacitor Android shell + dev signing | 2–2.5 | existing app in the native shell on a real device |
| **M0.5** Security gate | helmet, rate-limiting (scope corrected), token-storage review, onboarding-gate hardening (server + client), session refresh + token-storage seam — **remaining: Keystore plugin** | ~1 | same app, safe on real devices |
| **M-Connect** Get-paid | Stripe Connect Express onboarding + charge routing | 2–3 | connect a payout account (web + Android) |
| **M1** Core natives | QR + manual fallback, GPS-on-walk-start, push, camera/photos | 3–3.5 | scan a tag → track a real walk → photo → push |
| **M2** Tap-to-Pay | Stripe Terminal in-person card flow | 2–3 | a real in-person card tap on the phone |
| **M3** Depth & polish | calendar, local notifications, widget, one-handed | 2–2.5 | feels native, not wrapped |
| **M-Brand** Logo + icon | brief, trademark check, the mark, palette v2, adaptive icon, splash, web icon set (B-1…B-4, B-8, N-1…N-3) | ~1 design + an afternoon of assets — **runs in parallel**, not on the code critical path | the app stops wearing the stock Android robot |
| **M-Play** Submission | Play Console listing, data-safety, signing, internal → closed test | ~1 + Google gate | live in Play internal/closed testing |

- **First testable on a phone:** ~2–2.5 wks (M0).
- **QR + GPS + push + photos:** ~6–7 wks in (later if M-Connect precedes M1).
- **Everything submission-ready:** ~**13–16 wks** with Connect in the first cycle; ~11–13.5 if Connect is deferred to founder-only testing. **M-Brand does not extend this** — it's parallel design work, provided it starts early enough to be done before M-Play.
- **Uncertainty drivers:** GPS foreground service, Tap-to-Pay device eligibility, and Google's ~14-day / ~20-tester **new-account gate** — create the Play account **early** so its clock runs in parallel with the M-phases. **M-Brand now shares that "start it early because something else gates it" property**: it blocks M-Play, so a late start on the mark stalls submission just as surely as the account gate does.

- [ ] **QA checkpoint M:** full E2E (signup → client → contract → sign → book → complete → invoice → pay) executed **on a real Android phone**, plus the device matrix from Workstream Q.

### Phase 3 — iOS port + Biometric Login *(deferred, scoped when Phase 2 is testing)*
The **iOS port** is the main Phase 3 driver — it's what needs a **Mac or cloud build environment** and re-entry into **Apple's review** (where Phase 2's native-feature depth pays off against guideline 4.2). **Biometric Login** rides here too (it's small and also works on Android — cheap to pull into the Android build later if wanted; kept in Phase 3 per founder call 2026-07-19). Rough size when picked up: **~5–8 weeks**.

- [ ] **Add `.gitattributes` when the second build machine appears** *(noted 2026-08-17)* — the repo is currently clean (all 142 tracked text files are stored **LF**; `core.autocrlf=true` gives the Windows working copy CRLF, which is correct and harmless). But that behavior lives in local git config, not in the repo, so it only holds while there's **one** machine. A Mac or cloud build env is the moment it can diverge: a contributor with `autocrlf=false` puts CRLF into the index, producing whole-file phantom diffs — and `android/gradlew`, the repo's only shebang script, **breaks outright if it lands with CRLF on Unix**. Pin it then (a no-op renormalization today, since everything is already LF):
  ```
  printf '* text=auto\ngradlew text eol=lf\n*.sh text eol=lf\n*.bat text eol=crlf\n' > .gitattributes
  ```
  Deliberately *not* done now: it would put an unrelated file into a security-focused review, and there is no second machine to diverge from yet.

## Workstream U — UI/UX Polish & Modernization

*The Phase 1 UI was built to be demoable, not delightful. This workstream is a deliberate design pass — informed by real feedback from the Week 8 demo and the owner-portal cold tester.*

> ⚠️ **Scope overlap with Phase 4 (added 2026-08-23).** `PHASE_4_ROADMAP.md` now owns brand + visual design (the logo, palette v2, type scale, the design language, the screen-by-screen redesign). The **"Design-system tightening"** bullet below duplicates its V-1…V-3/V-6 almost word for word. **Open decision D4-1:** move that bullet to Phase 4 and keep U as the pure usability/a11y pass — or let U do the *mechanical* tokenization (today's values) and let Phase 4 supply the new ones. Either way U's **accessibility pass should re-run after Phase 4**, since new colors and type invalidate every contrast measurement taken before them. ~~Two Phase 4 items are also **candidates to pull forward into Workstream M**~~ — **decided 2026-08-23 (D4-4): pulled.** The mark (B-1…B-4), the trademark check (B-8) and the Android icon + splash + web icon set (N-1…N-3) now live in **Workstream M → M-Brand** above. ⚠️ **This makes the D4-1 question sharper, not looser:** M-Brand ships **palette v2 (B-4)** while U's design-system bullet is still unassigned, so if U tokenizes against today's colors it will be re-tokenizing against B-4's within the same phase. Settle D4-1 before starting U's design-system bullet.

- [ ] Heuristic review of every screen against a written checklist: empty states, loading states, error states, destructive-action confirmations, success feedback, back-navigation coherence — log findings as checklist items here
- [ ] Design-system tightening: extract the ad-hoc CSS into named tokens (spacing, type scale, radii, palette already exists per `SPEC.md`), one card/button/form vocabulary used everywhere, consistent light/dark parity
- [ ] Accessibility pass: labels and roles (the a11y tree is already decent), focus states, contrast in both themes, keyboard-only walkthrough of the core flow
- [x] Modernization decision — **vanilla JS vs. a build step** — **DECIDED 2026-07-19: adopt Vite + TypeScript** for the front end (keeps the no-framework simplicity, adds modules/type-safety/hot-reload); reach for a framework only if state management genuinely hurts. Stays one deployable — no separate front-end service. `public/app.js` was a single growing file that owner portal + messaging + mobile layouts would roughly double; this is also the pipeline Workstream M's Capacitor wrap needs.
- [ ] Micro-UX debt from the Phase 1 walkthrough not already scheduled in Weeks 7–8: clickable `tel:`/`mailto:` everywhere client contact info appears, truncated text affordances, timezone display sanity check
- [ ] Onboarding wizard (P2-5) lands here once the design pass sets the visual vocabulary
- [ ] **QA checkpoint U:** cold-user test (repeat of the Week 8 exercise — someone unfamiliar drives the app without help), plus the device matrix

## Workstream X — Multi-Profession Infrastructure (trainers, groomers, sitters, boarding)

*The architecture was built for this from day one — `account_type` enum, generic `ServiceType`, profile-mapped offerings (Week 6), generic billing. Phase 2's job is to finish the last mile: the places where the **UI and copy** still assume "dog walker."*

- [ ] Terminology audit: the UI says "walks"/"walk report"/"No walks" in places where the service might be grooming or a training session — make labels service-type-aware (data model needs nothing; this is presentation). The event name `walk_completed` stays (append-only log; renaming events breaks history) — map it to friendly copy at display time
- [ ] Contract template library: one seeded CA dog-walking agreement exists today. Add per-profession starter templates (training, grooming, sitting/boarding) with the same merge-field system — founder sources/reviews the legal text, same legal-counsel notice as Phase 1
- [ ] Service-type-aware completion reports: the Week 6 walk report (times, notes, 🐶/🦴) is the template — grooming wants before/after photos (seam: P2-3), training wants progress notes, boarding already has per-day stays. Extend the completion form per `ServiceType` without forking the appointment model
- [ ] Boarding/sitting stay UX: multi-day stays exist in the model (per-day billing, non-exclusive time) — give them check-in/check-out date UI instead of the walk-shaped time picker
- [ ] Profession-aware onboarding: P2-5's wizard sets `offered_service_types` at signup so a groomer's first session already looks like a grooming app
- [ ] Seam-integrity check (do this last, it's the marketplace insurance): no hardcoded `account_type` checks crept in, all significant actions still publish events, billing still treats products as "billable items attached to an account"
- [ ] **QA checkpoint X:** run the full E2E flow as three personas — walker, groomer, boarder — each seeing only their world (service types, labels, templates)

## Workstream S — Subscription Tiers: Just Me / Essential / Business

*Added 2026-07-15 at founder direction: access to PetPro Connect becomes a paid signup with three account tiers. This is the use case the `CLAUDE.md` "generic billing" seam was explicitly built for — a professional's subscription is a billable item attached to their account, running through the same Stripe machinery as their clients' invoices, not a parallel system. Subscription revenue goes to the founder's Stripe account (unaffected by the P2-6 Stripe Connect question, which concerns client-payment payouts).*

- [ ] Founder defines the **tier matrix** (see Decisions Queue — blocks everything below): which features belong to each tier, monthly price, annual option, free-trial policy. Working assumption to react against, based on the names: **Just Me** = solo professional, core workflow (clients, contracts, scheduling, invoicing); **Essential** = solo + the premium conveniences (auto walk reports, branded invoices, calendar sync, vaccination tracking); **Business** = everything + multi-staff
- [ ] Model tiers in Stripe Billing: three Products with recurring Prices; new `subscriptions` type + tier field on the account (deliberate additions to `src/types/index.ts`, flagged per `CLAUDE.md`)
- [ ] Signup flow: choose tier → Stripe Checkout in subscription mode → account activates on webhook confirmation (same signature-verification + idempotency pattern as Week 5). Trial-first vs. card-up-front is a founder call in the tier matrix
- [ ] **Entitlement middleware**: one central "does this account's tier include this feature" check, same discipline as `account_type` — never scattered hardcoded tier checks in service code, so re-packaging tiers later is a config change, not a refactor
- [ ] Subscription lifecycle handling: payment failed → grace period → **read-only lockout, never data deletion** (a walker's client records are their business — losing them over a failed card would be unforgivable); cancel and downgrade behavior defined per the tier matrix
- [ ] Billing settings screen (extends the Profile tab): current plan, upgrade/downgrade with Stripe's proration, payment-method update + receipts via the Stripe customer portal (no card data ever on our server, as always)
- [ ] ⚠️ **"Business" likely implies multi-staff** — employees under one business account is a real data-model expansion (who owns clients, whose schedule shows whose walks, per-seat pricing). If the founder confirms that's what Business means, it gets scoped as its own workstream item rather than smuggled in here
- [ ] Grandfathering: founder's own account, demo accounts, and any pre-tier signups get flagged exempt so turning tiers on can't lock out existing users
- [ ] **QA checkpoint S:** E2E with a test card — sign up on each tier → entitlements enforced correctly → simulate payment failure → grace → lockout → recovery → cancel; tier changes apply exactly once (idempotent webhooks). Entitlement enforcement also gets added to **Security review #2's** scope, since a tier check is an access-control boundary

## Workstream F — Feature Backlog Build-Out

*The detailed specs live in `ROADMAP.md` → "Phase 2 Backlog" (P2-1…P2-11) — they are not duplicated here. This is the priority ordering, revised as the founder learns from real users.*

> **Moved to Phase 3 (2026-07-19):** P2-1, P2-7, P2-9, P2-11 (and P2-12, P2-14, which were never tiered here) were relocated to **`PHASE_3_ROADMAP.md`** at founder direction and dropped from the tiers below. P2-2 stayed — Workstream M depends on it.

**Tier 1 — quick wins, mostly thin UI over existing APIs (candidates to interleave early):**
- [ ] P2-10 No-show & late-cancel fee flow
- [ ] P2-5 Signup onboarding wizard *(scheduled inside Workstreams U + X above)*

**Tier 2 — client-facing value, needs founder decisions first:**
- [ ] **Nitro Sign electronic signing (the "Phase 1.5" deferral from Week 4)** — the full plan is preserved in `ROADMAP.md` Week 4's collapsed section; goes through `IeSignProvider` as always
- [ ] P2-2 Walk-report auto-message on completion (decisions: SMS vs email/in-app first; check-in mechanism)
- [ ] P2-3 Photos + notes on walk reports
- [ ] P2-4 Calendar sync (iCal feed first)

**Tier 3 — big lifts, gated on explicit founder decisions:**
- [ ] P2-6 Per-professional payouts → **Stripe Connect** (the marketplace payment seam; the non-Stripe-processor variant needs a `CLAUDE.md` constraint amendment and stays parked)
- [ ] P2-8 In-person tap-to-pay (native-app-sized; interim QR/embedded-Checkout options documented in the backlog item)
- [ ] **QA checkpoint F:** each shipped Tier item extends `npm test` with its own leg before it's called done

---

## Proposed Sequencing

*A proposal, not a contract — the founder reorders as priorities emerge from the demo.*

| Order | What | Why first |
|---|---|---|
| 1 | Workstream 0 + Security review #1 | Clean, audited base before anything is built on it |
| 2 | Workstream D (account lifecycle & data privacy) | Foundational: M-Connect's connected-account seam depends on it, and data-deletion is a compliance surface worth having before real users arrive. Pairs with Security review #1 |
| 3 | Workstream M (mobile) | Highest-leverage gap for real walkers; everything after inherits it |
| 4 | Workstream U (polish) + Tier 1 features interleaved | Demo feedback will be fresh; Tier 1 items are small enough to ride along |
| 5 | Workstream X (professions) + Security review #2 | Opens the market; the second security review catches what expansion changed |
| 6 | Workstream S (subscription tiers) | Turn on paid signup once the product is polished and mobile-ready enough to be worth paying for — gating signups behind a paywall before then costs adoption. Founder can pull it earlier if revenue timing demands |
| 7 | Tiers 2–3 by founder priority | Each needs decisions logged in the queue below |

## Founder Decisions Queue

*Decisions Phase 2 needs from you, none blocking today. Each gets logged with a date when made, same habit as Phase 1.*

- [x] **Test-data strategy** (unblocks CI in Workstream 0) — **DECIDED 2026-07-19, REVISED 2026-07-19: separate Supabase test project** (supersedes the earlier cleanup-script pick). Rationale: a live cleanup can only deactivate + PII-scrub, **never delete** — the append-only `events` log (008 trigger) and the no-hard-delete rule mean every CI/e2e run permanently accretes rows in production. A separate project can be reset/dropped and lets migrations be **rehearsed** before they touch prod. CI/dev point at it via one env var; keep migrations applied there too (one extra target — migrations are already applied by hand). A lightweight cleanup routine can still live *inside* the test project as a convenience, not as the prod-safety mechanism it can't be. Decoupled from Workstream D.
- [x] **Front-end modernization** (Workstream U / Workstream M) — **DECIDED 2026-07-19: adopt Vite + TypeScript** as the modernization path. Stays one deployable (no separate front-end service); this is also the build pipeline Capacitor's Android wrap needs, so it's on the mobile critical path.
- [x] **PWA vs. native timing** — **DECIDED 2026-07-19: native app, Android-first** (Capacitor wrap + native features), iOS in Phase 3. Supersedes the PWA-first plan. See Workstream M.
- [x] **QR check-in mechanism** (P2-2) — **DECIDED 2026-07-19: QR with a manual "Start walk" fallback.** QR is an option the walker can use, never a dependency; candidate for a paid tier.
- [x] **Payout account type** (Workstream M / P2-6) — **DECIDED 2026-07-19: Stripe Connect Express.** Requirement: the connected account must be changeable/disconnectable if the professional relationship is terminated — not hardwired (build behind the P2-15 deactivation seam — now **Workstream D**).
- [ ] **Payment setup: block vs. fall back** (Workstream M / M-Connect): until a walker completes Connect onboarding, is collecting payment **blocked**, or does it **fall back to the platform account**? — OPEN
- [ ] **Platform fee** (Workstream M / M-Connect): does PetPro take a percentage of each transaction, or pass 100% to the walker? — OPEN
- [x] **RLS as a second isolation net** (Security review #1) — **DECIDED 2026-07-19: approved.** Add Postgres RLS policies as a second net beneath the app-code tenant scoping (the owner portal is live, so the second-account-type case the recommendation waited on is now real). Scoped as migration work inside Security review #1.
- [x] **Deactivated professional's clients** (Workstream D) — **DECIDED 2026-07-19: retain, portal access unaffected.** Client rows and the owners' portal login are untouched when a professional deactivates; wherever the walker is surfaced to an owner, they **display as "deactivated."** (Orphan-but-retain — they're real people with signed agreements.)
- [x] **Pull the logo + Android icon into Workstream M?** (`PHASE_4_ROADMAP.md` **D4-4**) — **DECIDED 2026-08-23: yes.** Scheduled as **M-Brand** above (B-1…B-4, B-8 + N-1…N-3). Play will not accept the stock Android robot, and it's ~a week of design running in parallel with the code work.
- [ ] **"PetPro" vs "PetPro Connect" — which is the brand?** (`PHASE_4_ROADMAP.md` **D4-6**) — OPEN, and **now near-term**: M-Brand's **B-2** cannot draw a wordmark or lockup without it. Today the app is inconsistent — `manifest.webmanifest` and `strings.xml` say "PetPro Connect", `short_name` says "PetPro". Also settle how the answer relates to the verified sending domains (`eastwestoak.com`, `itchytail.com`).
- [ ] **Who designs the mark?** (`PHASE_4_ROADMAP.md` **D4-2**) — OPEN, and now near-term for the same reason: founder + Claude Code, a contract designer for the identity only (B-1…B-4), or a designer for all of Phase 4. Materially changes M-Brand's timeline and cost. A mark that ends up on every invoice, email, and store listing is a reasonable place to spend money once.
- [ ] **eSign timing**: when does Nitro Sign (Phase 1.5) actually start — before or after mobile?
- [ ] **Tier matrix** (blocks Workstream S): which features land in Just Me vs. Essential vs. Business, monthly prices, annual discount yes/no, free trial vs. card-up-front
- [ ] **What "Business" includes**: if it means multiple staff under one account, that's a data-model expansion to scope separately (seats, client ownership, per-staff schedules)
- [ ] **When tiers switch on**: proposed as step 6 in sequencing (after polish + mobile) — pull earlier only if revenue timing demands it
- [ ] **Which professions launch first** in Workstream X (affects which contract templates to source)
- [ ] *(carried from Phase 1, still open)* Invoice timing for weekly/monthly-billed services

## Status at a Glance

| Workstream | Status |
|---|---|
| 0 — Closeout & cleanup | **In progress** — separate test project + CI done (2026-08-01); cleanup pass, doc refresh (`PHASE_1_SUMMARY.md`/`ARCHITECTURE.md`), dependency upgrades, and QA checkpoint 0 remaining |
| Q — QA & security reviews | **Security review #1 largely done** (2026-08-01: tenant audit, helmet/rate-limit/CORS, cross-tenant tests, RLS on test **and** prod, prod DB password rotated, `KEY_ROTATION.md`); **open:** service-key rotation + Render env check. Review #2 pending |
| D — Account lifecycle & data privacy | **DONE 2026-08-01** — deactivation + contact-PII scrub + auth-user deletion + M-Connect seam (migration 024); self-service on both sides (`/api/auth/deactivate`, `/api/portal/deactivate` + Profile "Close my account"); retained clients + "deactivated" walker in the portal; QA checkpoint D = e2e step 16 (green). **Not yet committed/pushed** |
| M — Mobile (Android-first native) | **In progress** — M0 foundation shipped; M0.5 security gate built (open: Keystore plugin); open blocker **M0-IMG** (HEIC upload). **M-Brand added 2026-08-23** (logo + Android icon pulled forward from Phase 4 per D4-4) — not started, runs in parallel, blocks M-Play. M-Connect / M1 / M2 / M3 not started |
| U — UI/UX polish | Not started |
| X — Multi-profession | Not started |
| S — Subscription tiers | Not started (blocked on tier-matrix decision) |
| F — Feature backlog | Not started |

*Phase 3 work (iOS port + Biometric Login, and the six backlog items moved 2026-07-19) is tracked in **`PHASE_3_ROADMAP.md`**. Design + brand work (logo, design language, app redesign, icon/splash/store creative) is tracked in **`PHASE_4_ROADMAP.md`** — note its overlap with Workstream U (D4-1) and the two items proposed to pull forward into Workstream M.*

## Changelog

- **2026-08-23 (later)** — **Logo + Android icon pulled forward into Workstream M as `M-Brand`** (founder decision, `PHASE_4_ROADMAP.md` **D4-4**). Scope: **B-1** brand brief (founder-authored, goes first), **B-8** trademark check (must clear before the mark is finalized), **B-2** name/lockups, **B-3** the mark, **B-4** palette v2, **N-1** adaptive icon incl. the Android 13+ monochrome layer, **N-2** splash, **N-3** PWA/web icon set. Rationale: the launcher icon and splash are still stock Capacitor/Android Studio defaults, which **Play will not accept** — a submission blocker parked in a design phase that hasn't started. **Two additions to Phase 4's original recommendation:** N-3 (same source SVG, same asset-generation sitting; the manifest ships one unmaskable icon today) and B-8 (a trademark conflict found *after* the mark is on a listing invalidates icon, splash, and store creative together). **Convention:** specs stay in `PHASE_4_ROADMAP.md` under permanent IDs, the schedule lives here, tick both. **Not on the code critical path** — parallel design work, added to the phase table without extending the 13–16 wk estimate, but it **blocks M-Play**, so it inherits the same "start early" property as the Play account gate. **Accepted consequence:** the mark precedes typography (B-5, still Phase 4 with open licensing decision D4-3), so the wordmark/lockups may want a second pass — the symbol the icon uses is unaffected. **Two Phase 4 decisions promoted to near-term as a result:** **D4-6** ("PetPro" vs "PetPro Connect" — B-2 can't draw a wordmark without it; the app is currently inconsistent between `manifest.webmanifest`/`strings.xml` and `short_name`) and **D4-2** (who designs it). Also sharpened the Workstream U overlap note: M-Brand ships palette v2, so tokenizing U against today's colors would mean doing it twice — settle **D4-1** first.
- **2026-08-23** — **`PHASE_4_ROADMAP.md` created** (app design + logo), and this file annotated where it touches: a scope-overlap note on **Workstream U** (its design-system bullet duplicates Phase 4's V-1…V-3/V-6 — resolution is open decision D4-1) and a pointer under Status at a Glance. Phase 4 recommends pulling the mark (B-1…B-4) and the **Android adaptive icon + splash (N-1…N-3)** forward into **Workstream M** before any Play submission — the launcher icon and splash are still the stock Capacitor/Android Studio defaults, which Play will reject. No Phase 2 scope or status changed.
- **2026-08-01 (later)** — **Workstream D (Account Lifecycle & Data Privacy) built.** Deactivation as `AccountService.deactivateAccount()`: flip `status → 'deactivated'` + new `deactivated_at` (migration `024`), scrub the leaver's own contact PII (email → unique tombstone, phone → null, professional `bio` / owner `address`), null the new nullable `stripe_connect_account_id` (**M-Connect seam** — Workstream D is sequenced first precisely so this link exists), and delete the Supabase auth user then sever `auth_user_id`. `requireAuth` already 403'd non-active accounts; added a status guard to `authenticateProfessional` so re-login is refused too. **PII-scrub scope decided "contact-only"** (founder, 2026-08-01) — client records are retained untouched, honoring the retain-clients decision. Self-service on both sides: `POST /api/auth/deactivate` (professional, password-confirmed) + "Close my account" in Profile; `POST /api/portal/deactivate` (owner, passwordless). `PortalService.overview()` now flags a deactivated walker so the portal shows "deactivated" (contact withheld, business name kept) with the client retained. **QA checkpoint D = e2e step 16** (deactivate a professional *and* an owner; session killed + re-login blocked + auth user gone + PII scrubbed + connect seam nulled; signed contract/invoice/clients/owner-portal all survive) — full `npm test` green (16/16). Typecheck + structure checks green. Migration 024 applied to the **test** project only; **not yet committed, pushed, or applied to prod.**
- **2026-08-01** — **Workstream 0 (test project + CI) and Security review #1 shipped to `main` behind green CI.** Stood up the separate Supabase **test** project (`dzejaycyjjaqajmatmjs`) and repointed local `.env` + CI at it, closing the tests-write-to-prod leak (all 23 migrations applied). Added **GitHub Actions CI** (Node 24 — which surfaced that the app needs Node ≥22 for supabase-js Realtime; `engines` corrected): migrate → boot server → full e2e, on push and PR. **Security review #1:** the tenant-isolation audit found app-code scoping already correct across every service — added `helmet` + a CSP tuned to the frontend (browser-verified), rate limiting on auth/portal-login/pay-link/webhook, default-deny CORS, and a **cross-tenant regression test** (e2e step 15); validated list query params; verified no secrets in git history; wrote the key-rotation drill (`KEY_ROTATION.md`). **Applied migrations 022 + 023 to prod** (rehearsed on test first) — **RLS is now live on all 26 prod tables**. **Rotated the prod DB password** (verified). Still open: rotate the exposed prod `SUPABASE_SERVICE_KEY` (scheduled — it invalidates all sessions) and confirm Render env least-privilege.
- **2026-07-19 (revision)** — **Test-data strategy flipped to a separate Supabase test project**, superseding the cleanup-script pick logged earlier the same day. Reason: a live cleanup can only deactivate + PII-scrub (append-only `events` + no hard delete), so prod permanently accretes test rows; a separate project can be reset and lets migrations be rehearsed before touching prod. **Decoupled from Workstream D** — deactivation is no longer the test-cleanup mechanism, so Workstream D's early sequencing now rests on the M-Connect seam alone. Also holding migration 022 (PH-2) off prod until the test project exists, to rehearse the backfill there first.
- **2026-07-19 (decisions)** — Three Founder-Decisions-Queue items resolved: **Front-end modernization = Vite + TypeScript** (Workstream U path, also the pipeline Capacitor needs); **Test-data strategy = cleanup script** against the live Supabase project (built on Workstream D's deactivation path, not a separate test project); **RLS as a second isolation net = approved** (migration work inside Security review #1). Folded into Workstreams 0, U, M and Security review #1 so the bodies match the queue.
- **2026-07-19 (later still)** — **New Workstream D — Account Lifecycle & Data Privacy** added at founder direction ("data privacy and deletion is important"). Schedules the previously-unscheduled **P2-15** (delete/deactivate) as deactivation + PII scrub + Supabase auth-user removal, with retention guardrails for signed contracts/invoices/transactions and a QA checkpoint folded into Security review #1. Sequenced as step **2** (before Workstream M) because M-Connect's connected-account link is built on its deactivation seam and it gives the e2e suite a cleanup path. Added a Status-at-a-Glance row, a Decisions-Queue item (fate of a deactivated professional's clients), and re-pointed the two M-Connect P2-15 seam references at Workstream D.
- **2026-07-19 (later)** — **Six backlog items moved to Phase 3** at founder direction, into a new **`PHASE_3_ROADMAP.md`**: P2-1 (walker ratings), P2-7 (branded invoices), P2-9 (record outside-Stripe payments), P2-11 (full pet profile + vaccination UI), P2-12 (bring-your-own contract upload), P2-14 (default price/duration per service type). Dropped from Workstream F's tiers; their `ROADMAP.md` specs are now redirect stubs. IDs kept as `P2-x` (stable references). **P2-2 stayed in Phase 2** — Workstream M's QR check-in + GPS are built on it. The camera/photos M-item keeps its Capacitor plugin in Phase 2 but notes the P2-7/P2-11 UIs it feeds are now Phase 3.
- **2026-07-19** — **Workstream M rewritten as an Android-first native app** at founder direction, superseding the PWA-first plan. Capacitor wrap + native features pulled forward from the backlog: QR check-in with manual fallback (P2-2), GPS walk tracking that starts only on walk start (P2-2), Tap-to-Pay on Android for testing (P2-8), push, camera/photos (P2-3/7/11), calendar (P2-4), local notifications + widget. New **M-Connect** item closes a gap found in onboarding — no way for a professional to set up getting paid (all money currently routes to the platform account); **Stripe Connect Express** chosen, with a requirement that the connected account be changeable/disconnectable on relationship termination (P2-15 seam). **iOS + Biometric Login moved to Phase 3** (Mac/cloud build env + Apple review). Estimates: first testable on a phone ~2–2.5 wks; submission-ready ~13–16 wks with Connect in the first cycle. Decisions logged: native/Android-first, QR-with-fallback, Stripe Express (all decided); **block-vs-fall-back** and **platform fee** left OPEN. Feature-gating note added so Phase 2 features sit behind clean entitlement checks for the Workstream S tiers.
- **2026-07-15 (later)** — **Workstream S added at founder direction: paid signup with three account tiers — Just Me, Essential, Business** — built on the generic-billing seam via Stripe Billing subscriptions, with central entitlement checks, never-delete-data lockout policy, and its own QA checkpoint. Three new items in the Founder Decisions Queue (tier matrix; whether Business means multi-staff; when tiers switch on) and a sequencing row (proposed step 5, after polish + mobile). Entitlement enforcement added to Security review #2's scope.
- **2026-07-15** — Document created at founder direction: workstreams 0/Q/M/U/X/F defined; Phase 1 backlog P2-1…P2-11 tiered into Workstream F; security posture baselined (no RLS, no helmet/rate-limiting — scheduled into review #1); code baseline verified (typecheck clean, zero audit vulnerabilities, all work committed). Phase 1 Weeks 7–8 still run under `ROADMAP.md` first.
